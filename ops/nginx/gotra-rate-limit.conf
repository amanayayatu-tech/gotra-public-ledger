limit_req_zone $binary_remote_addr zone=gotra_api:10m rate=5r/s;
limit_req_zone $binary_remote_addr zone=gotra_reports:10m rate=2r/s;
limit_req_status 429;

map $request_method $gotra_proxy_method {
    HEAD GET;
    default $request_method;
}
